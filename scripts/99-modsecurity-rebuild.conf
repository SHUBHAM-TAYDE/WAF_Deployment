# =============================================================================
# /etc/apt/apt.conf.d/99-modsecurity-rebuild
# APT Post-Invoke Hook for ModSecurity-NGINX Connector Auto-Rebuild
# =============================================================================
# Trigger check-and-rebuild script after any dpkg action
DPkg::Post-Invoke {
    "/bin/bash /opt/ModSecurity/WAF_GUI/scripts/check-and-rebuild.sh";
};
